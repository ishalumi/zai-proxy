services:
  zai-proxy:
    image: ghcr.io/ishalumi/zai-proxy:latest
    container_name: zai-proxy
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "127.0.0.1:${PORT:-7990}:${PORT:-7990}"
